# Tech Debt Register

Known, deliberate shortcuts — with the reason they exist and what unlocks fixing them. Reviewed when planning each milestone; add an entry instead of a `// TODO` so debt is visible in one place.

| #   | Debt                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Why it exists                                                                                                                                                                                                                                                                     | Unblocked by / fix                                                                                                                                                                                                                                     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | ~~`ComposerSheetView` uses private `PrimaryButtonStyle`/`SecondaryButtonStyle` that look different from the design-system `PrimaryButton`/`SecondaryButton`.~~ **RESOLVED 2026-07-10:** swapped to the shared capsule components; the private styles are deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                                                                                                                                                                 | Done. (Row kept for stable numbering.)                                                                                                                                                                                                                 |
| 2   | ~~`ProfileService.libraryItems` fetches all kinds and filters client-side.~~ **RESOLVED 2026-06-15:** kind filter pushed server-side via `media.kind=eq.…` on the `media!inner` join; syntax verified against the remote PostgREST API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                                                                                                                                                                                                                                                                 | Done. (Row kept for stable numbering; other docs reference items by number.)                                                                                                                                                                           |
| 3   | `FeedPostRow` (FeedService) and `LibraryItemRow` (ProfileService) decode nearly identical post+media rows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Wire formats are deliberately service-private; the overlap is tolerated.                                                                                                                                                                                                          | If a third copy appears, extract a shared row DTO next to `SupabaseSchema`.                                                                                                                                                                            |
| 4   | Cover images ride `AsyncImage` + `URLCache` rather than a dedicated image cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Zero-dependency start; TMDB/OpenLibrary/Cover Art Archive all send cacheable responses.                                                                                                                                                                                           | Re-add Kingfisher only if scroll performance or cache hit-rate demands it.                                                                                                                                                                             |
| 5   | Follow-scoped feed fetches the followee list then filters posts with an `in (…)` list — two round trips, and the URL grows with follow count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Reuses the proven PostgREST embed query; an RPC would need json-shape work the client can't integration-test yet.                                                                                                                                                                 | When typical follow counts pass a few hundred (or feed pagination lands): move the join into a `feed_posts` RPC.                                                                                                                                       |
| 6   | Periphery (dead-code scan) is configured but not in CI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | macOS CI minutes are expensive; the codebase is still small.                                                                                                                                                                                                                      | Run `make periphery` locally before big refactors; add a CI job when the team grows.                                                                                                                                                                   |
| 7   | A `memory/` folder with personal info existed in git history before PR #84 untracked it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Committed accidentally by an AI pair session.                                                                                                                                                                                                                                     | If the repo ever goes public: scrub history with `git-filter-repo` first.                                                                                                                                                                              |
| 8   | ~~UI tests cover the tab renders and the people-search → public-profile journey, but no auth, onboarding, or composer journeys.~~ **RESOLVED 2026-07-22:** onboarding was already covered (`-previewOnboarding`); added `-previewAuth` and `-previewComposer` stub-backed shells + 4 UI tests for the magic-link send and log/watchlist flows.                                                                                                                                                                                                                                                                                                                                                                         | —                                                                                                                                                                                                                                                                                 | Done. (Row kept for stable numbering.)                                                                                                                                                                                                                 |
| 9   | `SupabaseSchema.swift`'s `ProfilesSchema.Row`/`FollowsSchema.Row` were hand-extended with `is_private`/`status` instead of via `make codegen`. **The private-accounts migration was `db:push`ed to the live DB on 2026-07-30** (needed to unblock this PR's `follow_counts` test failure), so the lenient-decode fallbacks (default `false`/`'accepted'`) are now permanently-safe rather than temporary, but `make codegen` still hasn't been run for real against the live schema to confirm it matches the hand edit.                                                                                                                                                                                               | No container runtime fit on disk to run `db:reset` + `make codegen` this session (recurring disk-space constraint on this machine).                                                                                                                                               | Run `make codegen` for real once disk space allows, confirm it matches the hand edit — no functional risk either way, just housekeeping.                                                                                                               |
| 10  | ~~Web `/[username]` page (Phase 2): no "Your Venn" section heading and no friendly empty-state copy when viewer and other have both logged nothing.~~ **RESOLVED 2026-08-04:** the overlap now renders inside a titled "Your Venn" section, and a both-sides-empty summary shows iOS's "Log a few things and your shared taste shows up here." instead of a bare 0/0 diagram.                                                                                                                                                                                                                                                                                                                                          | Whole-branch review caught this as a rule-17 parity gap after all 10 plan tasks were already implemented and reviewed; fixing it wasn't one of the two Important (must-fix) findings so it was deferred rather than reopening review.                                             | Add the heading + empty-state branch to `web/app/[username]/page.tsx` / `web/components/VennOverlap.tsx`, mirroring `PublicProfileView.swift`'s `overlapContent`.                                                                                      |
| 11  | Web `VennOverlap.tsx`'s "in common" legend row doesn't bold the count the way the label is bolded; iOS bolds both. Also the shared-count chip is a fixed 32×24 SVG rect (could clip a 3-digit count) where iOS uses a content-sized capsule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Same as #10 — caught late in whole-branch review, both are cosmetic/rare-path.                                                                                                                                                                                                    | One-line font-weight fix for the legend count; make the chip `rx`/width responsive to digit count (or switch to a `<foreignObject>`/HTML overlay) if 3-digit shared counts become common.                                                              |
| 12  | ~~Web `FollowButton`'s follower/following counts on `/[username]` go stale after a successful follow.~~ **RESOLVED 2026-08-04:** the button calls `router.refresh()` after a successful follow-to-accepted and after an unfollow, so the server-rendered counts — and the shelves/overlap gating that depends on follow status — re-fetch.                                                                                                                                                                                                                                                                                                                                                                             | Same whole-branch-review timing as #10/#11.                                                                                                                                                                                                                                       | Call `router.refresh()` (or lift counts into client state and bump them optimistically) after `requestFollow` resolves to `"accepted"` in `web/components/FollowButton.tsx`.                                                                           |
| 13  | ~~Web E2E suite (`web/e2e/`) has no authenticated-session fixture.~~ **RESOLVED 2026-08-05:** `e2e/auth.setup.ts` signs a dedicated user in through the app's own code-entry form and saves the cookie jar for a separate `authenticated` Playwright project. The session is real, so those specs exercise RLS, the onboarding gate, and the signed-in pages end to end rather than through a mocked client. The service-role key lives in one GitHub Actions secret; forks never receive it, and there the authenticated specs skip instead of failing. No email is ever sent -- codes come from `admin.generateLink`, which mints a token without delivering one. Setup is documented in `docs/DEPLOY.md` section 7. | Building a real fixture meant a dedicated test user plus a service-role key somewhere in CI -- new infrastructure and new security surface, out of proportion to Phase 2's scope.                                                                                                 | Extend the fixture to seed deterministic content (a post, a follow edge) when a spec needs to assert on rows rather than on structure. Today's specs deliberately avoid depending on a shared project's data, which comes and goes.                    |
| 14  | Web's `/login` accepts a numeric code (`supabase.auth.verifyOtp`) as a fallback to the magic-link; iOS's `AuthService`/`AuthView` only has `sendMagicLink`/`handleCallback`, no code entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Web-specific pain: a magic link's redirect depends on the exact origin being allow-listed in Supabase, which breaks constantly across local dev ports; the code sidesteps redirects entirely. iOS's deep-link scheme doesn't have that problem, so it wasn't built there too.     | Add `verifyOTP(email:token:)` to iOS's `AuthServicing` + a code-entry field to `AuthView` if the team wants parity; not urgent since iOS's magic link isn't flaky the way web's local dev redirect is.                                                 |
| 15  | ~~Web's onboarding gate in `web/lib/supabase/proxy.ts` runs a `profiles` lookup on **every** request from a signed-in user.~~ **RESOLVED 2026-08-05:** an httpOnly cookie holding the user id short-circuits the lookup once a profile exists, so it runs at most once per user per browser. Storing the id rather than a boolean stops a previous account's answer carrying over after a sign-out; the cookie is deliberately unsigned because forging it only skips the onboarding redirect, and RLS still refuses to return any data.                                                                                                                                                                               | The proxy is the only place that can gate every route centrally (iOS's `OnboardingGate` equivalent); Next.js gives it no per-session cache, and adding one meant either a cookie to keep in sync or a request-level memo that doesn't survive across requests. Correctness first. | Set a signed "profile complete" cookie when onboarding finishes and skip the query when it's present; or move the check into a shared layout. Worth doing before the site sees real traffic — it's a DB round-trip on the hot path of every page load. |
| 16  | ~~A brand-new web user who follows nobody sees the empty feed with no in-app way out.~~ **RESOLVED 2026-08-04:** the composer lets them log something, and Explorer's People search lets them find and follow others. Web's feed empty state still omits iOS's "Find them under People in the Explorer tab — or log something yourself." sentence; restoring it is a one-line copy change now that both surfaces exist.                                                                                                                                                                                                                                                                                                | Phase ordering — the feed ships before Explorer and the composer. Pointing users at features that aren't there would be worse than the shorter copy.                                                                                                                              | Ships with Explorer / people search. Restore iOS's full empty-state sentence at that point so rule 17 copy parity holds exactly.                                                                                                                       |

| 17 | ~~Web has no hosting — there is no URL anyone can open.~~ **RESOLVED 2026-08-05:** deployed to Vercel at https://venn-social.vercel.app (and https://venn-theta.vercel.app), with all three environment variables set. Remaining: no custom domain, so the URL is a `*.vercel.app` subdomain — see row 20, which blocks professional email. | The Phase 1 spec planned Vercel's free tier and it was never set up. `docs/DEPLOY.md` is the runbook; it needs a Vercel account, which is a founder task. | Set it alongside the Supabase env vars when web is first deployed. CI does not need it: E2E tests never call the endpoint, and the key is read per request rather than at build time. |

| 18 | ~~iOS's Explorer labels its browse panel "Recommended for you", but `ExplorerService.recentMedia` returns the newest catalog rows with no scoring.~~ **RESOLVED 2026-08-06:** real recommendation shelves now sit above the browse grid on both platforms, each labelled for what it actually is. The browse grid remains as the fallback beneath them and no longer claims to be a recommendation. | Matching the copy would have shipped a claim the code could not support — there was no ranking of any kind on either platform. | Done. |

| 19 | ~~Both platforms' **Year in Review** shows a trailing twelve months, not a calendar year — the name implies a retrospective the data doesn't produce.~~ **RESOLVED 2026-08-14.** The screen is called **Last 12 Months** on both platforms, which is what the query returns. Renamed rather than reworked, per this row's own guidance not to overload it. | The accessibility label already said "trailing twelve months" while the visible heading said "Year in Review", so screen-reader users were told the truth and sighted users were not. That is a strange way round, and it is what made the rename the obvious fix rather than a judgement call. | Done. A seasonal "your 2026 in venn" is still a good idea and still a different feature: a different query, a different page, and a moment rather than a screen. Build it as that, not by reinterpreting this one. |

| 20 | **Auth email runs on Supabase's built-in sender, which throttles at ~3–4 sends/hour** and returns `over_email_send_rate_limit` (429). The app reports that as the generic "Couldn't send the magic link. Please try again.", so a throttle is indistinguishable from a bug. At launch, three or four sign-ups in the same hour would lock everyone else out. Emails also come from a Supabase address with no venn branding. | Custom SMTP needs a verified sending domain (SPF/DKIM/DMARC), and `*.vercel.app` can't be verified — Vercel owns that DNS. The name isn't settled yet, so buying a domain was deliberately deferred. | Buy a domain → verify it with Resend (3k/month free) → set Supabase SMTP → paste `supabase/templates/magic_link.html` into the dashboard's email templates. Full sequence in `docs/DEPLOY.md` §6. **Do this before launch**, and consider surfacing the 429 distinctly in `web/app/(auth)/login/page.tsx` so a throttle stops looking like a failure. |
| 21 | ~~Comments are **flat** — no replies-to-replies on either platform.~~ **RESOLVED 2026-08-16.** `parent_id`, replies exactly one level deep, on both platforms. Replies notify the parent's author with a new `reply` kind rather than reusing `comment`, which renders as "commented on your post" and would be wrong for someone answering a remark on a stranger's post. | The row asked for a depth cap and a recursive read. A cap of one removes the recursion entirely — every comment is a root or a reply to a root, so a flat fetch is grouped in the client and there is no recursive CTE to get wrong. Deep threads are also close to unreadable on a phone. The cap is enforced by trigger, not by the clients, so a reply to a reply cannot arrive from a mistaken insert. | Done. Note `notifications_target_matches_kind` had to be widened too: its ELSE branch required both target columns to be null, so every reply notification would have violated it — and silently, because notify_post_commented swallows exceptions so a notification failure never costs someone their comment. |
| 22 | ~~Comments **can't be edited**, only deleted and reposted.~~ **RESOLVED 2026-08-15.** An author can edit their own comment, and every edit is marked. `edited_at` is stamped by a trigger, not the client, and only when the body actually changed, so the marker cannot be forged or suppressed. A second guard pins `post_id`, `author_id` and `created_at`, so an update cannot move a comment to another post or another name. The UPDATE policy is the author alone — unlike delete, the post's author is excluded, because removing a comment from your post is moderation and rewriting it is impersonation. | The original objection stands and is exactly why the marker exists: an edited comment in a thread someone already replied to rewrites the conversation. Marking it keeps that honest. | **No edit history was built.** The marker is what protects a reader; a history nobody can view is storage with no purpose. If the trail is ever wanted it wants a UI in the same change. Edits are also not rate-limited, unlike inserts — low severity, since RLS scopes them to your own rows and no notification fires, but worth a row of its own if it ever matters. |
| 23 | ~~**List items can't be reordered** on either platform. `list_items` has no position column, so a list is always in insert order.~~ **RESOLVED 2026-08-06 — and the premise was wrong.** `list_items.position` has existed since lists shipped, along with an owner-scoped write policy already covering UPDATE. No schema change was needed; what was missing was a way to set it. Both platforms now have move-up/move-down controls backed by `reorder_list_items(_list_id, _media_ids)`, which rewrites the whole order in one statement. | The row was written from memory rather than from the schema, and nobody checked. Worth remembering: a tech-debt entry claiming something is missing deserves the same verification as a spec claiming something exists. | Done. Drag-to-reorder can replace the buttons later on the same RPC — the arrows were chosen over drag because web drag-and-drop needs either a new dependency or fragile HTML5 DnD with poor touch support, and buttons are accessible on both platforms for free. |
| 24 | ~~**iOS can't add anything to a list.**~~ **RESOLVED 2026-08-06:** iOS now has both of web's surfaces. The composer lands on a confirmation step after logging and offers "Also add to a list" (`AddToListPicker`), and a list's own screen has an Add button that searches the catalog and appends (`ListAddView`). `ComposerServicing` gained `upsertMedia(candidate:)`, exposing what `log` already did internally — `list_items.media_id` references `public.media`, so a catalog result nobody has logged has no row to reference. | Web's composer got the affordance first (PR 151) and the iOS composer wasn't updated in the same session — a rule 17 gap logged rather than left silent. | Done. Reordering the result is still row 23. |
| 25 | ~~When a catalog provider is unreachable, **iOS's media detail screen offers a retry and web silently shows less**.~~ **RESOLVED 2026-08-15.** Web now says so and offers a retry, replacing only the enriched sections exactly as iOS does. `loadMediaDetail` marks a failed fetch `unavailable`, which is what makes "we could not ask" distinguishable from "there is nothing to show" — identical in the data, completely different to a reader. | The row proposed a client island for the enriched sections. That would have meant a client fetching from our own API, and therefore re-introducing the catalog detail route deleted in row 26 — buying a recovery affordance with a new endpoint that spends TMDB quota. | Done, and by a better route than the one suggested here. The retry is `router.refresh()`, which re-runs the Server Component and re-attempts the provider call in-process. Same recovery, no new surface, API key stays server-side. |
| 26 | ~~`web/app/api/catalog/detail/route.ts` is **unreachable dead code**.~~ **RESOLVED 2026-08-15.** Deleted. Confirmed unreferenced first: nothing in `web/`, `ios/` or the E2E suite called it, and its three helpers (`regionFrom`, `sourceUrlFor`, `EMPTY_DETAIL`) all have other consumers, so none became dead with it. Its sibling `api/catalog/search` is very much alive and untouched. | It was written first, then bypassed by the SSRF/cookie-forwarding fix in the same PR. The exposure was small but real: any signed-in user could spend the TMDB quota at 120 req/min on a feature that no longer existed. | Done. |

| 28 | **Notifications are in-app and pull-only.** There is no APNs push on iOS, no Web Push, and no email digest, so you only learn about activity by opening the app. ~~The badge refreshes when the shell appears rather than live — Supabase Realtime is available and unused.~~ **Badge resolved 2026-08-15:** both platforms subscribe to `notifications` and re-read the count on any change. The `supabase_realtime` publication was empty, so subscribing alone would have changed nothing; the table is now published with `replica identity full` so UPDATE and DELETE payloads carry enough for RLS to filter them. | Push still needs an Apple Push certificate, an Edge Function to send, and a device-token table — none of which I can create. Realtime was the half worth doing first, exactly as this row suggested. | **Push remains, and is blocked on you** for the certificate. Both platforms re-read rather than incrementing, so a notification marked read on another device lands correctly too. |

| 29 | ~~**`recommendation_feed` has no automated test.**~~ **RESOLVED 2026-08-16.** `supabase/tests/recommendation_feed_rls_test.sql` covers the private-account boundary, and a new `RLS tests` workflow runs it — and the private-accounts harness beside it, which had never run in CI — on every PR touching `supabase/`. | The row proposed pgTAP or a seeded CI database. Neither was needed: the private-accounts harness had already established a lighter pattern that builds its own base schema, applies the real migration with `\\i`, and fails via RAISE EXCEPTION. That runs on a plain `postgres:17-alpine` (~80MB, seconds to start) rather than the ~6GB Supabase image. | Done. Seven assertions, including that both functions are still `security invoker` — the one-word change that would break every other guarantee at once — and that a _pending_ follow request grants nothing, which is the case easiest to conflate in a predicate. Two tests exist purely so the others cannot pass by returning nothing. |
| 30 | **`similar_users` scans posts across all users on every call.** Free at current scale, not free at ten thousand users. | A materialised similarity table needs a refresh schedule and an invalidation story; building it before anyone can measure the problem would be guessing. | Materialise the similarity scores on a schedule. `recommendation_feed`'s signature does not change, so this is an internal swap when query time starts to show. |
| 31 | **`ExplorerViewModel` and `ExplorerService` are dead code.** They existed only to load the "Recently added" grid, which was removed on 2026-08-18. Their unit tests still pass, which is what makes this easy to miss — a green suite over code nothing calls. | Removing files is approval-gated in this repo, so they were left in place when the grid went and the caller was flagged for review instead. | Charles's go-ahead to delete `ios/Venn/Features/Explorer/ExplorerService.swift`, `ExplorerViewModel.swift`, their tests, and `web/lib/explore.ts`'s `fetchRecentMedia`. Confirm no other caller first — the last check found none. |
| 32 | **Nothing protects the public Supabase env values from a bad paste.** `web/lib/supabase/env.ts` now takes the first line and trims, which is why the 2026-08-17 outage (the anon key pasted twice with a newline between the copies, making every `fetch` throw before it left the browser) cannot recur in that exact form. But nothing _checks_ the deployed values, so a wrong-but-well-formed key would still fail silently at runtime. | The bug was invisible from the backend: Supabase logged no requests, because none arrived. It cost a day of sign-ins being impossible for everyone. | A startup assertion or a smoke test that signs in against the deployed preview on every deploy. Cheap version: a CI step that decodes the anon key's JWT payload and fails if `ref` doesn't match the expected project. |
| 33 | **The omnidirectional feed is web-only.** `/feed` can be read as a pannable plane of covers (the "Everywhere" mode); iOS has only the vertical list. This is a rule 17 gap, logged rather than left to drift. | The interaction is built on two-axis overflow scrolling, pointer capture and DOM virtualisation. SwiftUI's equivalent is a different construction rather than a port — most likely a `ScrollView([.horizontal, .vertical])` over a `Canvas`-backed or lazily-populated grid — so it is its own piece of work, not a translation. | An iOS `SpatialFeedView` behind the same per-user preference. Worth doing only if the mode gets used; if it does not, the honest fix is removing it from web rather than porting it. |
| 34 | **Two avatar helpers are now dead code.** Both platforms position and zoom an avatar before uploading it, so nothing calls `web/lib/avatarImage.ts` or `ios/Venn/Utils/AvatarImage.swift` any more. `AvatarImageTests` still passes, which is what makes this easy to miss — a green suite over code nothing calls. | Deleting is approval-gated in this repo, so the unused helpers were left rather than removed alongside their last callers. | Charles's go-ahead to delete both files and `ios/VennTests/Utils/AvatarImageTests.swift`. The cropper's own geometry is covered on both sides by `avatarCrop.test.ts` / `AvatarCropTests`. |
| 35 | **The secondary-surfaces menu is a wheel on web and a list on iOS.** Web fans Settings / Lists / Activity / Last 12 Months onto an arc that unfurls from the button; iOS still presents them as rows. A rule 17 gap, logged rather than left to drift. | The wheel is built on measuring the toggle's viewport position and animating four absolutely-positioned links out of it. SwiftUI's version is its own construction — most likely offsets driven by a spring animation — rather than a port. | An iOS radial menu, or a decision that the wheel was a web-only flourish and iOS keeps rows. Worth deciding rather than drifting. |
| 36 | **`useGridReorder` is dead code, and iOS still reorders by drag.** Web moved reordering into the ⋯ menu — pick "Reorder", then tap where it goes — so nothing calls the drag hook any more. iOS's shelves still drag. | Deleting is approval-gated, so the hook was left rather than removed alongside its last caller. The iOS half is a rule 17 gap: the same interaction is a different construction in SwiftUI, not a port. | Charles's go-ahead to delete `web/components/useGridReorder.ts`, plus the same menu entry on iOS. |

## Figma backlog

CLAUDE.md rule 15 (Figma-first) is suspended until the Figma Pro subscription lands. Every net-new UI surface shipped during the suspension is listed here so it gets recreated in Figma for completeness:

- Explorer "People" category: chip, browse prompt, search result rows (`ProfileRow`)
- `PublicProfileView` — other-user profile (header + bio + follow button + shelf tabs + cover gallery)
- Follow button states (Follow / Requested / Following) on the public profile
- Locked-content state on a private account's public profile (`PublicProfileView`'s `lockedContent`) — shown to non-followers instead of the shelves/overlap
- `FollowRequestsView` — pending follow-request list with inline accept/reject, reached from a new icon on `ProfileView`'s top bar (private accounts only)
- `FollowListView` — followers / following list (reuses `ProfileRow`)
- Real cover art treatments: `MediaCoverTile` image state, `MediaCoverThumb` row thumbnails
- "Your Venn" overlap section — **rebuilt in code 2026-06-24** (`VennOverlap`): proportional lobes + neon-blue lens via `Canvas` (replacing the invisible `.plusLighter` version), a Jaccard "NN% match" headline, and a shared-count chip. Light/dark verified. Remaining for a Figma pass: a more modern visual treatment of the diagram (depth, motion, typography) when Figma returns.
- `OnboardingView` — claim-username screen after first sign-in (fields mirror AuthView)
- Avatar photo treatments: `AvatarBadge` image state, edit-sheet avatar row + photo picker
- Sign-in revamp: "check your inbox" screen (bold email, resend countdown, change-email)
- Stepped onboarding: step indicators, live username availability (✓/✗ + hint line), photo step with circular picker hero
- `SettingsView` — account settings sheet (private-account toggle today; grows as more settings land)
- `YearInReviewView` — personal stats screen (total-logged header, trailing-12-month activity bars, per-kind breakdown cards), reached from a new icon button in `ProfileView`'s top bar
- Web `/login`'s numeric-code fallback form (code input + verify button, shown alongside the "check your inbox" state) — web-only, no iOS equivalent (see tech-debt row 14)
- Web `/profile/year` — Year in Review: total-logged header, trailing-12-month bar chart, per-kind cards, and the empty state
- Web `/explorer` — category chips, search field, people result rows, media result rows. (The "Recently added" browse grid it used to list was **removed 2026-08-18**: the per-kind tabs now show recommendation shelves narrowed to that kind. Nothing to design for the old grid.)
- Web `/composer` — the log flow (kind chips, search field, result rows with cover thumbs, the Love/Like/Dislike chips, caption field, and the Log it / Add to watchlist pair), plus the accent "Log" nav button
- Web `/feed` — the feed stream (attribution row with avatar + relative time, cover, title/metadata, rating, caption) and its "Quiet for now" empty state
- Web `AppNav` — the persistent Feed / Explorer / Profile nav on signed-in pages, including the disabled Explorer treatment
- Web `/media/[id]` — the media detail page (cover + title header with runtime/rating facts and a "Log this" button, About, "Where to watch" provider chips, Author/Artist/Directed by, Cast list, genre chips, and the link out to the source catalog)
- iOS `MediaDetailView` — the same screen natively, plus the two states web has no equivalent for: the loading caption and the retryable error that replaces the enriched sections only
- iOS feed social footer — `PostActionsView` now renders inline under every `FeedRow` (previously the whole row was one link to the permalink); needs a frame for the like + comment-tally row in feed context
- iOS `ProfileView` "See all" — the text button beside the shelf tabs that opens the full editable library, added because covers now open the title instead
- Web `/notifications` and iOS `NotificationsView` — the Activity screen (rows with avatar, actor name, the event sentence, a quoted comment where there is one, relative time, and the tinted unread state), plus its "Nothing yet" empty state
- The unread badge in both shells — iOS's `.badge` on the Activity tab, web's capped "9+" pill in `AppNav`
- Web icon set (`components/Icon.tsx`) — heart (outline/filled), comment bubble, star, thumbs up/down, tick, cross, plus. Hand-drawn SVGs standing in for iOS's SF Symbols; a designer should decide whether these are the real ones or whether the app adopts a licensed set
- The `--color-like` / `Theme.Color.like` red — the first non-accent colour in the palette, introduced so a liked heart doesn't read as a link. Worth a designer's eye on the exact value in both schemes
- Web `AppNav`'s compose action as an icon-only accent pill (was the word "Log")
- Feed attribution: the author's name now renders in the accent colour as a link, with the action verb left secondary — on both platforms
- Recommendation shelves on both platforms — the horizontal cover rows, their headings, the loading state, and the quiet error treatment that leaves the browse grid usable
- Web `/onboarding` — the two-step claim-username + add-photo flow (step label, username field with live ✓/✗ availability, display-name field, circular photo picker with preview, skip affordance). Mirrors iOS's `OnboardingView`/`OnboardingPhotoView` in copy and structure, but is its own responsive web layout and needs its own frames.
- "Where to **read**" and "Where to **listen**" on both platforms — the same provider-chip row as "Where to watch", but for books and albums, with its own heading and a footnote saying these are searches rather than an availability claim. Shipped 2026-08-17
- The **inline comment thread** under a feed row, on both platforms — the expanded state of the comment tally: the divider, the thread itself (roots, indented replies, edit/reply/delete affordances), the composer, and the loading and failed-to-load lines. Previously only ever seen on the permalink, which had a heading this one deliberately drops. Shipped 2026-08-18
- Explorer's **per-kind recommendation tabs** and their "No recommendations yet" empty state, which replaced the "Recently added" grid on both platforms. Shipped 2026-08-18
- The **"Everywhere" feed** — the pannable plane of covers, its cards (artwork plus attribution), the drift that offsets each column, and the List / Everywhere switch above the feed. Web-only, and the largest net-new surface since the Venn overlap. Shipped 2026-08-25
- The **launch splash on both platforms** — the still three-lobe mark, held for a beat. The brand video it used to play was dropped on 2026-08-26 (it did not look good), so what needs a frame is the mark, its size, and the fade.
- **Shelf arrows** — the circular chevron buttons that flank a horizontal row in place of a scrollbar, including their disabled-at-the-end state. Shipped 2026-08-25
- The **floating nav** — `AppNav` lost its bottom rule and gained a translucent, blurred ground so it reads as hovering over the page. A re-skin of an existing surface rather than a new one, but it changes how every signed-in page is framed. Shipped 2026-08-25
- The **avatar cropper** — the round window a photo is dragged and zoomed inside, its zoom control, and the Use photo / Cancel pair. Shared by profile editing and onboarding step 2. Shipped 2026-08-26
