# Tech Debt Register

Known, deliberate shortcuts — with the reason they exist and what unlocks fixing them. Reviewed when planning each milestone; add an entry instead of a `// TODO` so debt is visible in one place.

| #   | Debt                                                                                                                                                                                 | Why it exists                                                                      | Unblocked by / fix                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `ComposerSheetView` uses private `PrimaryButtonStyle`/`SecondaryButtonStyle` that look different from the design-system `PrimaryButton`/`SecondaryButton` (rounded-rect vs capsule). | Composer shipped before the button design settled.                                 | Figma decision on the canonical button (rule 15), then swap to the shared components.                                                |
| 2   | `ProfileService.libraryItems` fetches all kinds and filters client-side.                                                                                                             | PostgREST embedded-resource filter (`media.kind=eq.…`) wasn't verified when built. | Verify the `!inner` join filter against staging, then push the filter server-side.                                                   |
| 3   | `FeedPostRow` (FeedService) and `LibraryItemRow` (ProfileService) decode nearly identical post+media rows.                                                                           | Wire formats are deliberately service-private; the overlap is tolerated.           | If a third copy appears, extract a shared row DTO next to `SupabaseSchema`.                                                          |
| 4   | `MediaCoverTile` renders tonal placeholders even though `Media.coverURL` now exists.                                                                                                 | Cover art rendering (caching, placeholder fallback) deserves its own pass.         | Add the image path (re-add Kingfisher if caching is needed — it was removed while unused) and keep the tile as placeholder fallback. |
| 5   | Feed is global, not follow-scoped.                                                                                                                                                   | The follow graph has no UI yet; a global feed makes dogfooding possible.           | Follow system ships → switch `FeedService.recentPosts` to a follow-graph RPC (protocol stays the same).                              |
| 6   | Periphery (dead-code scan) is configured but not in CI.                                                                                                                              | macOS CI minutes are expensive; the codebase is still small.                       | Run `make periphery` locally before big refactors; add a CI job when the team grows.                                                 |
| 7   | A `memory/` folder with personal info existed in git history before PR #84 untracked it.                                                                                             | Committed accidentally by an AI pair session.                                      | If the repo ever goes public: scrub history with `git-filter-repo` first.                                                            |
| 8   | UI test coverage is a stub (`VennUITests` exercises little beyond boot).                                                                                                             | Core flows were still churning.                                                    | First stable end-to-end flow (auth → log → feed) gets a real XCUITest journey.                                                       |
